# 1. Используем стандартный образ Node.js 18 (на базе Debian, часто решает проблемы SSL)
FROM node:18

# 2. Устанавливаем рабочую директорию внутри контейнера
WORKDIR /usr/src/app

# 3. Копируем package.json и package-lock.json (если есть)
COPY package*.json ./

# 4. Устанавливаем ВСЕ зависимости (включая devDependencies)
# Используем 'npm install' вместо 'ci', т.к. он может быть менее строгим к окружению
RUN npm install

# 5. Копируем весь остальной код вашего приложения
COPY . .

# 6. Собираем TypeScript в JavaScript (результат будет в ./dist)
RUN npm run build

# 7. Указываем порт, который будет слушать ваше приложение (Express)
# Убедитесь, что этот порт (5000) совпадает с тем, что вы указали в .env или в коде Express
EXPOSE 5000

# 8. Команда для запуска приложения при старте контейнера
# Запустит скрипт "start" из вашего package.json (который должен быть 'node dist/app.js')
CMD [ "npm", "start" ]